window.onload=function(){
  splitWords();
  renderCanvas();

  
    // let zoomElement = document.querySelector(".first>img")
    // let zoom = 1;
    // let zoom_speed = 0.1;

    // document.addEventListener("wheel",function(e){
    //   if (e.deltaY > 0){
    //     zoomElement.style.transform = `scale(${(zoom += zoom_speed)})`;
    //   } else{
    //     zoomElement.style.transform = `scale(${(zoom -= zoom_speed)})`;
    //   }
    // });
    


}